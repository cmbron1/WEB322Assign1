<section>
    
    <div class="row3">
        <div class="column3">
            {{#each products3}}
            <img class="item" src="{{this.image}}">
            <p id="prtitle">{{this.title}} <br> {{this.price}} <br> {{this.category}} <br> {{this.best}}</p>
           <!-- <p id="prprice">{{this.price}}</p>
            <p id="prcat">{{this.category}}</p>
            <p id="prbest">{{this.best}}</p>-->
            {{/each}}
        </div>
    </div>
</section>